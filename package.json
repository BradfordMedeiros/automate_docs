{
  "name": "docs",
  "scripts": {
    "start": "gitbook serve",
    "build": "rm -rf _book && gitbook build",
    "build_and_deploy": "yarn run build && gh-pages -d _book",
    "deploy": "gh-pages -d _book"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "gitbook-cli": "^2.3.0"
  }
}
